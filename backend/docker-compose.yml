services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: aaryamart-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: aaryamart_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - aaryamart_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d aaryamart_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - aaryamart-network

  redis:
    image: redis:7-alpine
    container_name: aaryamart-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - aaryamart_redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - aaryamart-network

  api:
    build:
      context: .
      dockerfile: src/ECommerce.API/Dockerfile
    container_name: aaryamart-api
    restart: unless-stopped
    environment:
      ConnectionStrings__PostgreSQL: "Host=postgres;Port=5432;Database=aaryamart_db;Username=postgres;Password=root;"
      ConnectionStrings__Redis: "redis:6379"
      ASPNETCORE_ENVIRONMENT: "Development"
      ASPNETCORE_URLS: "http://+:5062"
    ports:
      - "5062:5062"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aaryamart-network

  gateway:
    build:
      context: .
      dockerfile: src/ECommerce.Gateway/Dockerfile
    container_name: aaryamart-gateway
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      ASPNETCORE_URLS: "http://+:5000"
      ReverseProxy__Clusters__ecommerce-api__Destinations__primary__Address: "http://api:5062/"
    ports:
      - "5000:5000"
    depends_on:
      - api
    networks:
      - aaryamart-network

volumes:
  aaryamart_pgdata:
  aaryamart_redisdata:

networks:
  aaryamart-network:
    driver: bridge
